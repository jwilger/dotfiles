#!/usr/bin/env zsh
LS_VPN_SUBNET="inet 192.168.2."

ifconfig | ack $LS_VPN_SUBNET
if [[ $? -gt 0 ]]; then
  vpn-connect "LSVPN"

  until ifconfig | ack $LS_VPN_SUBNET;
  do
    sleep 1
  done

  flushcache
  sleep 1
  ping -t2 -c1 code.livingsocial.net
fi
ls_vpn_keepalive
exit 0
