#!/usr/bin/env bash

SESSION_NAME="$1"
if [ -z "$SESSION_NAME" ]; then
  SESSION_NAME=`basename $PWD`
fi
shift
OTHER_ARGS="$*"

tmux attach -t "$SESSION_NAME" $OTHER_ARGS || tmux new -s "$SESSION_NAME" $OTHER_ARGS
