import { FoldingRange } from 'vscode-languageserver-types';
export declare class DockerFolding {
    private createFoldingRange(foldingRangeLineFoldingOnly, startLine, endLine, startCharacter, endCharacter, kind?);
    private getLineLength(document, line);
    computeFoldingRanges(content: string, lineFoldingOnly: boolean, limit: number): FoldingRange[];
}
