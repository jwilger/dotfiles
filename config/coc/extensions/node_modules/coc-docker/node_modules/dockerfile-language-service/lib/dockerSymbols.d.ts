import { SymbolInformation, TextDocumentIdentifier } from 'vscode-languageserver-types';
export declare class DockerSymbols {
    private createSymbolInformation(name, textDocumentURI, range, kind, deprecated);
    parseSymbolInformation(textDocument: TextDocumentIdentifier, content: string): SymbolInformation[];
}
